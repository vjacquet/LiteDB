name: CI

on:
  pull_request:
  workflow_dispatch:

jobs:
  build-and-test:
    uses: ./.github/workflows/_reusable-ci.yml

  repro-runner:
    uses: ./.github/workflows/reprorunner.yml
    secrets: inherit
